id: "Orchestrator"
status_complete: True   # values: status_complete, status_notcomplete, status_inprogress
tasks:
    parent:
        id: 3089
        progress: 80
    children:
        - name: "Repository synchronization"
          id: 3854
          progress: 100
        - name: "Code style specification"
          id: 3857
          progress: 100
        - name: "Unit testing coverage"
          id: 3425
          progress: 28
        - name: "Functional and integration testing coverage"
          id: 3428
          progress: 100
        - name: "Configuration management"
          id: 3092
          progress: 100
        - name: "GitBook documentation"
          id: 3098
          progress: 50
repository: 
    url:
        - https://github.com/indigo-dc/orchestrator.git
code_style:
    standard:
        - java_google 
    exceptions: 0
    jenkins_job:
        - orchestrator-codestyle
unit_test:
    coverage: 28
    jenkins_job:
        - orchestrator-unittest
    comments:
        - "Unit testing coverage has decreased from last report (30\\%)."
func_int_test:
    url:
    jenkins_job:
        - orchestrator-integration
    description:
        - "\\underline{Functional tests}: WF tasks related to the external Services, using mocks (CloudProviderRanker, CMDB, SLAM, Monitoring, Notification callbacks) - 40\\%."
        - "\\underline{Integration tests}: (without mock) for external services (CloudProviderRanker, CMDB, SLAM - we had some problems with the last couple, but we will fix them soon)."
gitbook:
    url: https://indigo-dc.gitbooks.io/orchestrator/content/
    types:
        - "Readme"
        - "API documentation"
        - "Deployment documentation"
    comments:
        - "Points to improve in the documentation:\\begin{itemize} \\item README \\begin{itemize} \\item Explain what CMF stands for \\item Elaborate on the role of the orchestrator within INDIGO infrastructure, from where are this high-level requests received, .. \\item Describe briefly the role of the dependant services (slam, cmdb, zabbix wrapper, cloudproviderranker), are all needed? \\item Explain what interfaces does the product support \\item Provide examples if needed \\end{itemize} \\item Deployment guide \\begin{itemize} \\item Add a note about deploying the container using indigodatacloud/orchestrator repository. \\item Explain why Chronos is needed, same for Onedata; what is their role, are they essential, etc. \\item How to get a Onezone token and the rest of values for onedata variables (examples or links could help here) \\end{itemize} \\item Create a new section with the REST APIs pointing to http://indigo-dc.github.io/orchestrator/restdocs/) \\end{itemize}"
config_management:
    wont_be_provided: True
    tool:   # e.g. ansible, ..
    url:
    level:  # values: installation, configuration, installation/configuration
    jenkins_job:
    build:
        - version: Ubuntu 14.04 LTS
          successful:   # True, False
          url:
        - version: CentOS7
          successful:   # True, False
          url:
    comments:
        - "The orchestrator is a dockerized service that runs on the kubernetes cluster, and the few configurations that it needs can be expressed via environment variables, so it doesn't really need any configuration management tool."
